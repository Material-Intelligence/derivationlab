// This file is auto-generated by @hey-api/openapi-ts

import * as z from 'zod/mini';

/**
 * AccountRateLimitWindowView
 */
export const zAccountRateLimitWindowView = z.object({
    kind: z.enum([
        'five_hour',
        'weekly',
        'other'
    ]),
    remaining_percent: z.int().check(z.gte(0), z.lte(100)),
    resets_at: z.nullish(z.string()),
    used_percent: z.int().check(z.gte(0), z.lte(100)),
    window_duration_mins: z.nullish(z.int().check(z.gt(0)))
});

/**
 * AccountRateLimitsView
 */
export const zAccountRateLimitsView = z.object({
    diagnostic: z.enum([
        'ready',
        'product_auth_missing',
        'rate_limits_unavailable'
    ]),
    observed_at: z.nullish(z.string()),
    plan_type: z.nullish(z.string().check(z.minLength(1), z.maxLength(256))),
    stale: z._default(z.optional(z.boolean()), false),
    status: z.enum([
        'available',
        'signed_out',
        'temporarily_unavailable'
    ]),
    windows: z.array(zAccountRateLimitWindowView).check(z.maxLength(2))
});

/**
 * AccountView
 */
export const zAccountView = z.object({
    credential_store: z.literal('file'),
    diagnostic: z.enum([
        'ready',
        'product_auth_missing',
        'product_auth_invalid',
        'source_auth_available',
        'source_auth_unavailable',
        'account_check_failed'
    ]),
    import_available: z.boolean(),
    status: z.enum([
        'signed_in',
        'signed_out',
        'reauth_required',
        'unavailable'
    ])
});

/**
 * ActiveCallOverlay
 */
export const zActiveCallOverlay = z.object({
    branchId: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    callId: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    fromStepId: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    label: z.string().check(z.minLength(1), z.maxLength(256))
});

/**
 * AdminCreateSiteAccountRequest
 */
export const zAdminCreateSiteAccountRequest = z.object({
    email: z.string().check(z.minLength(3), z.maxLength(254)),
    password: z.string().check(z.minLength(1), z.maxLength(128)),
    role: z._default(z.optional(z.enum(['user', 'admin'])), 'user'),
    username: z.string().check(z.minLength(3), z.maxLength(64))
});

/**
 * AdminResetSitePasswordRequest
 */
export const zAdminResetSitePasswordRequest = z.object({
    new_password: z.string().check(z.minLength(1), z.maxLength(128))
});

/**
 * AdminSetSiteAccountStatusRequest
 */
export const zAdminSetSiteAccountStatusRequest = z.object({
    status: z.enum(['active', 'disabled'])
});

/**
 * BackendConfig
 */
export const zBackendConfig = z.object({
    name: z.string().check(z.minLength(1), z.maxLength(256)),
    version: z.string().check(z.minLength(1), z.maxLength(256))
});

/**
 * BudgetView
 */
export const zBudgetView = z.object({
    maxSteps: z.nullable(z.int().check(z.gte(1))),
    usedSteps: z.int().check(z.gte(0))
});

/**
 * BuildInfoView
 *
 * Immutable identity shared by the app bundle and local backend.
 */
export const zBuildInfoView = z.object({
    build_number: z.string().check(z.regex(/^(?:0|\d{8}\.\d{6})$/)),
    commit: z.string().check(z.regex(/^[0-9a-f]{40}$/)),
    openapi_sha256: z.string().check(z.regex(/^[0-9a-f]{64}$/)),
    product_mode: z.enum(['development', 'release']),
    release_id: z.string().check(z.minLength(1), z.maxLength(256)),
    schema_version: z.literal('derivationlab-build-info-v1'),
    version: z.string().check(z.regex(/^(?:dev|\d{4}\.\d{1,2}\.\d{1,2})$/))
});

/**
 * CreateBranchRequest
 *
 * Discriminated human expansion command.
 *
 * A direction is free text.  A revision is direct human-authored scientific
 * content, so its instruction must be a JSON object with exactly the five
 * StepContent fields.  A runtime must canonicalize it before recording the
 * revise_step HumanAction; it must never ask a model to author content and
 * label that output as human.
 */
export const zCreateBranchRequest = z.object({
    from_step_revision_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    instruction: z.string().check(z.minLength(1), z.maxLength(16384)),
    kind: z.enum(['human_direction', 'human_revision'])
});

/**
 * CreateIntakeSessionRequest
 */
export const zCreateIntakeSessionRequest = z.object({
    effort: z.string().check(z.minLength(1), z.maxLength(256)),
    initial_message: z.string().check(z.minLength(1), z.maxLength(32768)),
    model: z.string().check(z.minLength(1), z.maxLength(256)),
    service_tier: z._default(z.optional(z.enum(['standard', 'fast'])), 'standard')
});

/**
 * DeviceLoginCancelView
 */
export const zDeviceLoginCancelView = z.object({
    status: z.enum(['canceled', 'not_found'])
});

/**
 * DeviceLoginStartView
 */
export const zDeviceLoginStartView = z.object({
    expires_at: z.string(),
    login_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    user_code: z.string().check(z.minLength(1), z.maxLength(128)),
    verification_url: z.string().check(z.maxLength(2048))
});

/**
 * DeviceLoginStatusView
 */
export const zDeviceLoginStatusView = z.object({
    diagnostic: z.nullish(z.string().check(z.maxLength(256))),
    status: z.enum([
        'pending',
        'signed_in',
        'failed',
        'expired',
        'canceled'
    ])
});

/**
 * EdgeView
 */
export const zEdgeView = z.object({
    from: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    kind: z.enum([
        'continuation',
        'model_fork',
        'human_direction',
        'human_revision',
        'model_revision',
        'proposed'
    ]),
    order: z.int().check(z.gte(0)),
    to: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))
});

/**
 * ErrorDetail
 */
export const zErrorDetail = z.object({
    code: z.string(),
    details: z.nullable(z.unknown()),
    message: z.string()
});

/**
 * ErrorEnvelope
 */
export const zErrorEnvelope = z.object({
    error: zErrorDetail,
    request_id: z.string()
});

/**
 * ExportReportRequest
 *
 * Select and explicitly confirm the main route for one immutable export.
 */
export const zExportReportRequest = z.object({
    confirm_selected_route: z.literal(true),
    selected_route_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))
});

/**
 * HealthView
 */
export const zHealthView = z.object({
    service: z.string(),
    status: z.enum(['ok', 'degraded'])
});

/**
 * ImportExistingAccountRequest
 */
export const zImportExistingAccountRequest = z.object({
    confirm_import: z.literal(true)
});

/**
 * IntakeAnswerInput
 *
 * One answer: chosen options, free text, or a ladder strategy.
 *
 * ``simplest_first`` and ``both_routes`` are answers in their own right, so an
 * input carrying only a strategy is complete. ``both_routes`` without an
 * explicit selection means every option the question offered.
 */
export const zIntakeAnswerInput = z.object({
    custom_text: z.nullish(z.string().check(z.minLength(1), z.maxLength(32768))),
    selected_option_ids: z._default(z.optional(z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))).check(z.maxLength(128))), []),
    strategy: z.nullish(z.enum([
        'selected',
        'simplest_first',
        'both_routes'
    ]))
});

/**
 * FinalizeIntakeSessionRequest
 *
 * Start with what is known: answer any pending problem question and go.
 *
 * Valid only while the session is ``convergence_required``. Every pending
 * problem question must be answered; everything still open becomes a declared
 * default the derivation agent owns.
 */
export const zFinalizeIntakeSessionRequest = z.object({
    answers: z._default(z.optional(z.record(z.string(), z.unknown())), {}),
    base_revision: z.int().check(z.gte(0))
});

/**
 * IntakeConvergenceView
 *
 * Server-side budget that decides when the grill stops asking.
 */
export const zIntakeConvergenceView = z.object({
    audit_rejections: z._default(z.optional(z.int().check(z.gte(0))), 0),
    finalized_by_user: z._default(z.optional(z.boolean()), false),
    reason: z.nullish(z.string().check(z.minLength(1), z.maxLength(32768))),
    rounds: z._default(z.optional(z.int().check(z.gte(0))), 0)
});

/**
 * IntakeConversationEventView
 */
export const zIntakeConversationEventView = z.object({
    event_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    kind: z.string().check(z.minLength(1), z.maxLength(256)),
    payload: z.record(z.string(), z.unknown())
});

/**
 * IntakeDecisionAnswerView
 */
export const zIntakeDecisionAnswerView = z.object({
    custom_text: z.nullable(z.string().check(z.minLength(1), z.maxLength(32768))),
    selected_option_ids: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    source_message_refs: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    strategy: z.nullish(z.enum([
        'selected',
        'simplest_first',
        'both_routes'
    ]))
});

/**
 * IntakeDeclaredDefaultView
 *
 * A convention or approximation the agent adopts instead of asking.
 */
export const zIntakeDeclaredDefaultView = z.object({
    alternatives: z._default(z.optional(z.array(z.string().check(z.minLength(1), z.maxLength(32768))).check(z.maxLength(32))), []),
    decision_class: z.enum([
        'problem',
        'convention',
        'approximation_level'
    ]),
    default_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    rationale: z.string().check(z.minLength(1), z.maxLength(32768)),
    statement: z.string().check(z.minLength(1), z.maxLength(32768)),
    title: z.string().check(z.minLength(1), z.maxLength(256))
});

/**
 * IntakeLadderRungView
 *
 * One refinement level; rung 0 is the textbook-simplest baseline.
 */
export const zIntakeLadderRungView = z.object({
    decision_ids: z._default(z.optional(z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))).check(z.maxLength(32))), []),
    default_ids: z._default(z.optional(z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))).check(z.maxLength(32))), []),
    name: z.string().check(z.minLength(1), z.maxLength(256)),
    parallel_branch: z._default(z.optional(z.boolean()), false),
    relaxes: z.string().check(z.minLength(1), z.maxLength(32768)),
    rung: z.int().check(z.gte(0))
});

/**
 * IntakePendingAnswerView
 *
 * One answer as the user submitted it, before any round accepted it.
 */
export const zIntakePendingAnswerView = z.object({
    custom_text: z.nullish(z.string().check(z.minLength(1), z.maxLength(32768))),
    selected_option_ids: z._default(z.optional(z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)))), []),
    source_message_refs: z._default(z.optional(z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)))), []),
    strategy: z.nullish(z.enum([
        'selected',
        'simplest_first',
        'both_routes'
    ]))
});

/**
 * IntakePendingSubmissionView
 *
 * What the user submitted on an attempt that never reached a commit.
 *
 * A round writes the user's answers and the model's reply together, so a model
 * round that fails takes the answers with it. The server keeps them here so
 * the client can offer them back instead of making the user retype them.
 */
export const zIntakePendingSubmissionView = z.object({
    answers: z._default(z.optional(z.record(z.string(), z.unknown())), {}),
    base_revision: z.int().check(z.gte(0)),
    failure_reason: z.nullish(z.string().check(z.minLength(1), z.maxLength(32768))),
    kind: z.enum(['round', 'finalize']),
    submitted_at: z.string().check(z.minLength(1), z.maxLength(256)),
    user_message: z.nullish(z.string().check(z.minLength(1), z.maxLength(32768)))
});

/**
 * IntakeProblemSectionView
 */
export const zIntakeProblemSectionView = z.object({
    content: z.nullable(z.string().check(z.minLength(1), z.maxLength(32768))),
    name: z.string().check(z.minLength(1), z.maxLength(32768)),
    not_applicable_reason: z.nullable(z.string().check(z.minLength(1), z.maxLength(32768)))
});

/**
 * IntakeProblemSpecificationView
 */
export const zIntakeProblemSpecificationView = z.object({
    critical_message_refs: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    declared_defaults: z._default(z.optional(z.array(zIntakeDeclaredDefaultView).check(z.maxLength(64))), []),
    refinement_ladder: z._default(z.optional(z.array(zIntakeLadderRungView).check(z.maxLength(32))), []),
    sections: z.array(zIntakeProblemSectionView),
    status: z.enum([
        'draft',
        'candidate_ready',
        'confirmed',
        'superseded'
    ]),
    supersedes_version: z.nullable(z.int().check(z.gte(1))),
    version: z.int().check(z.gte(1))
});

/**
 * IntakeQuestionOptionView
 */
export const zIntakeQuestionOptionView = z.object({
    impact: z.string().check(z.minLength(1), z.maxLength(32768)),
    label: z.string().check(z.minLength(1), z.maxLength(256)),
    option_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))
});

/**
 * IntakeQuestionView
 *
 * One problem-class decision the user must settle before the derivation runs.
 */
export const zIntakeQuestionView = z.object({
    allow_custom: z.boolean(),
    answer_mode: z.enum([
        'single_choice',
        'multi_choice',
        'choice_with_text',
        'text'
    ]),
    blocking: z.boolean(),
    decision_class: z._default(z.optional(z.enum([
        'problem',
        'convention',
        'approximation_level'
    ])), 'problem'),
    decision_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    depends_on: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    grounded_in: z.nullish(z.string().check(z.minLength(1), z.maxLength(32768))),
    options: z.array(zIntakeQuestionOptionView),
    prompt: z.string().check(z.minLength(1), z.maxLength(32768)),
    question_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    recommendation_reason: z.nullable(z.string().check(z.minLength(1), z.maxLength(32768))),
    recommended_option_ids: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    semantic_key: z.string().check(z.minLength(1), z.maxLength(32768)),
    title: z.string().check(z.minLength(1), z.maxLength(256)),
    why_it_matters: z.string().check(z.minLength(1), z.maxLength(32768)),
    why_needed: z.string().check(z.minLength(1), z.maxLength(32768))
});

/**
 * IntakeDecisionView
 */
export const zIntakeDecisionView = z.object({
    answer: z.nullable(zIntakeDecisionAnswerView),
    decision_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    question: zIntakeQuestionView,
    reopen_reason: z.nullable(z.string().check(z.minLength(1), z.maxLength(32768))),
    revision: z.int().check(z.gte(1)),
    semantic_key: z.string().check(z.minLength(1), z.maxLength(32768)),
    source_message_refs: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    status: z.enum([
        'open',
        'resolved',
        'superseded'
    ]),
    supersedes_revision: z.nullable(z.int().check(z.gte(1)))
});

/**
 * IntakeRevisionRequest
 */
export const zIntakeRevisionRequest = z.object({
    base_revision: z.int().check(z.gte(0))
});

/**
 * IntakeThreadGenerationView
 */
export const zIntakeThreadGenerationView = z.object({
    app_server_thread_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    generation: z.int().check(z.gte(1)),
    prompt_version: z.string().check(z.minLength(1), z.maxLength(256)),
    replaced_generation: z.nullable(z.int().check(z.gte(1))),
    replacement_reason: z.nullable(z.string().check(z.minLength(1), z.maxLength(32768))),
    status: z.enum([
        'active',
        'superseded',
        'closed'
    ])
});

/**
 * ModelOptionView
 *
 * One App Server model and its model-specific reasoning levels.
 */
export const zModelOptionView = z.object({
    default_effort: z.string().check(z.minLength(1), z.maxLength(256)),
    default_service_tier: z._default(z.optional(z.enum(['standard', 'fast'])), 'standard'),
    display_name: z.string().check(z.minLength(1), z.maxLength(256)),
    is_default: z.boolean(),
    model: z.string().check(z.minLength(1), z.maxLength(256)),
    supported_efforts: z.array(z.string().check(z.minLength(1), z.maxLength(256))).check(z.minLength(1), z.maxLength(16)),
    supported_service_tiers: z.optional(z.array(z.enum(['standard', 'fast'])).check(z.minLength(1), z.maxLength(2)))
});

/**
 * QuitReadinessView
 */
export const zQuitReadinessView = z.object({
    active_run_count: z.int().check(z.gte(0)),
    safe_to_quit: z.boolean()
});

/**
 * ReportBundleView
 *
 * Safe API projection of a versioned ReportBundle.
 *
 * Paths are repository-relative artifact locations produced by the service;
 * clients cannot supply or influence an output path.
 */
export const zReportBundleView = z.object({
    bundle_path: z.string(),
    export_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    files: z.array(z.string()),
    manifest: z.record(z.string(), z.unknown()),
    run_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    selected_route_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    status: z.enum(['success', 'failed'])
});

/**
 * RoleModelConfig
 */
export const zRoleModelConfig = z.object({
    effort: z.string().check(z.minLength(1), z.maxLength(256)),
    model: z.string().check(z.minLength(1), z.maxLength(256)),
    provider: z.string().check(z.minLength(1), z.maxLength(256))
});

/**
 * FrozenRunConfig
 *
 * Fields that map exactly into Record V1 run_created payloads.
 */
export const zFrozenRunConfig = z.object({
    allowed_paths: z.array(z.string()).check(z.maxLength(256)),
    backend: zBackendConfig,
    checker: zRoleModelConfig,
    checker_enabled: z._default(z.optional(z.boolean()), true),
    granularity: z.enum(['one_claim', 'one_task']),
    judge: zRoleModelConfig,
    max_active_branches: z.nullable(z.int().check(z.gte(1), z.lte(1000))),
    max_local_repairs: z._default(z.optional(z.int().check(z.gte(0), z.lte(10))), 3),
    max_model_calls: z.nullable(z.int().check(z.gte(1), z.lte(10000))),
    record_version: z._default(z.optional(z.enum(['1.0', '1.1'])), '1.0'),
    reference_allowed: z.boolean(),
    writer: zRoleModelConfig
});

/**
 * RunCommandCapabilities
 *
 * Backend-derived mutation affordances for one concrete Run snapshot.
 */
export const zRunCommandCapabilities = z.object({
    branchable_step_revision_ids: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    can_interrupt: z._default(z.boolean(), false),
    can_pause: z._default(z.boolean(), false),
    can_resume: z._default(z.boolean(), false)
});

/**
 * RunSummary
 *
 * Small catalog row for active and immutable archived runs.
 */
export const zRunSummary = z.object({
    created_at: z.string(),
    id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    phase: z.enum([
        'submitted',
        'autonomous_exploration',
        'human_expansion',
        'paused',
        'recovering',
        'review_ready',
        'review_ready_due_to_cap',
        'interrupted',
        'error'
    ]),
    question: z.string().check(z.minLength(1), z.maxLength(32768)),
    read_only: z.boolean(),
    route_count: z.int().check(z.gte(0)),
    status: z.enum([
        'running',
        'review_ready',
        'paused',
        'interrupted',
        'error'
    ]),
    step_count: z.int().check(z.gte(0)),
    updated_at: z.string()
});

/**
 * RuntimeConfig
 *
 * Implemented v1 execution profile stored outside frozen Record config.
 *
 * V1 exposes ChatGPT subscription authentication, the serial scheduler, and
 * one versioned benchmark capability profile. ``max_run_seconds`` remains an
 * explicit required null until the service enforces a wall-clock deadline.
 */
export const zRuntimeConfig = z.object({
    auth_mode: z.literal('chatgpt'),
    capability_profile: z._default(z.optional(z.enum(['benchmark_symbolic_v1', 'source_reading_v1'])), 'benchmark_symbolic_v1'),
    concurrency: z.literal(1),
    dimension_check: z._default(z.optional(z.boolean()), true),
    generation_context_sha256: z.nullish(z.string().check(z.regex(/^[0-9a-f]{64}$/))),
    intent_ledger_first: z._default(z.optional(z.boolean()), true),
    max_run_seconds: z.null(),
    reading_mode: z._default(z.optional(z.enum([
        'on_demand',
        'direct_full',
        'reader_assisted'
    ])), 'on_demand'),
    retries: z.union([z.literal(0), z.literal(1)]),
    service_tier: z._default(z.optional(z.enum(['standard', 'fast'])), 'standard')
});

/**
 * CreateRunDefaultsView
 *
 * Server-owned defaults for the product's create-run form.
 */
export const zCreateRunDefaultsView = z.object({
    allowed_efforts: z.array(z.string().check(z.minLength(1), z.maxLength(256))).check(z.minLength(1), z.maxLength(16)),
    allowed_models: z.array(z.string().check(z.minLength(1), z.maxLength(256))).check(z.minLength(1), z.maxLength(64)),
    config: zFrozenRunConfig,
    model_catalog_refreshed_at: z.nullable(z.iso.datetime()),
    model_catalog_source: z.enum([
        'app_server',
        'last_known_good',
        'static_fixture'
    ]),
    model_options: z.array(zModelOptionView).check(z.minLength(1), z.maxLength(64)),
    runtime: zRuntimeConfig
});

/**
 * CapabilitiesView
 */
export const zCapabilitiesView = z.object({
    api_version: z.literal('derivation-http-v1'),
    branch_kinds: z.array(z.enum(['human_direction', 'human_revision'])),
    command_transport: z.literal('http'),
    create_run_defaults: zCreateRunDefaultsView,
    event_transport: z.literal('sse'),
    idempotency_header: z.literal('Idempotency-Key'),
    phases: z.array(z.enum([
        'submitted',
        'autonomous_exploration',
        'human_expansion',
        'paused',
        'recovering',
        'review_ready',
        'review_ready_due_to_cap',
        'interrupted',
        'error'
    ])),
    replay_query: z.literal('follow=false'),
    sse_cursor_header: z.literal('Last-Event-ID')
});

/**
 * RuntimeOverlay
 */
export const zRuntimeOverlay = z.object({
    activeCalls: z.array(zActiveCallOverlay),
    hard_interrupt_requested: z.boolean()
});

/**
 * SiteAccountView
 */
export const zSiteAccountView = z.object({
    email: z.string().check(z.minLength(3), z.maxLength(254)),
    must_change_password: z.boolean(),
    role: z.enum(['user', 'admin']),
    status: z.enum(['active', 'disabled']),
    user_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    username: z.string().check(z.minLength(1), z.maxLength(256))
});

/**
 * SiteLoginRequest
 *
 * Website login input; password whitespace is never silently normalized.
 */
export const zSiteLoginRequest = z.object({
    identifier: z.string().check(z.minLength(1), z.maxLength(254)),
    password: z.string().check(z.minLength(1), z.maxLength(1024))
});

/**
 * SiteModeView
 */
export const zSiteModeView = z.object({
    channel: z.enum([
        'development',
        'preview',
        'stable',
        'staging'
    ]),
    mode: z.enum(['desktop', 'server'])
});

/**
 * SitePasswordChangeRequest
 *
 * Password rotation input; exact bytes are passed to the identity authority.
 */
export const zSitePasswordChangeRequest = z.object({
    current_password: z.string().check(z.minLength(1), z.maxLength(1024)),
    new_password: z.string().check(z.minLength(1), z.maxLength(128))
});

/**
 * SiteSessionView
 */
export const zSiteSessionView = z.object({
    absolute_expires_at: z.iso.datetime(),
    account: zSiteAccountView,
    idle_expires_at: z.iso.datetime()
});

/**
 * SourcePackRef
 *
 * Opaque reference-pack identity; no retrieval authority is implied.
 */
export const zSourcePackRef = z.object({
    pack_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    sha256: z.string().check(z.regex(/^[0-9a-f]{64}$/)),
    version: z.string().check(z.minLength(1), z.maxLength(256))
});

/**
 * FrozenProblemInput
 *
 * Versioned scientific input; direct input and Intake confirmation have distinct provenance.
 */
export const zFrozenProblemInput = z.object({
    accepted_decisions: z.optional(z.array(z.string().check(z.minLength(1), z.maxLength(32768))).check(z.maxLength(256))),
    allowed_references: z.array(z.string().check(z.minLength(1), z.maxLength(32768))).check(z.maxLength(256)),
    allowed_tools: z.array(z.string().check(z.minLength(1), z.maxLength(256))).check(z.maxLength(64)),
    assumptions: z.array(z.string().check(z.minLength(1), z.maxLength(32768))).check(z.maxLength(256)),
    confirmed_by_user: z.boolean(),
    declared_defaults: z._default(z.optional(z.array(zIntakeDeclaredDefaultView).check(z.maxLength(64))), []),
    deliverable: z.string().check(z.minLength(1), z.maxLength(32768)),
    givens: z.array(z.string().check(z.minLength(1), z.maxLength(32768))).check(z.maxLength(256)),
    objective: z.string().check(z.minLength(1), z.maxLength(32768)),
    origin: z._default(z.optional(z.enum(['confirmed_intake', 'direct_spec'])), 'confirmed_intake'),
    problem_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    refinement_ladder: z._default(z.optional(z.array(zIntakeLadderRungView).check(z.maxLength(32))), []),
    scope: z.string().check(z.minLength(1), z.maxLength(32768)),
    source_pack: z.nullish(zSourcePackRef),
    success_criteria: z.array(z.string().check(z.minLength(1), z.maxLength(32768))).check(z.minLength(1), z.maxLength(256)),
    supersedes_version: z.nullish(z.int().check(z.gte(1))),
    version: z.int().check(z.gte(1))
});

/**
 * CreateRunRequest
 */
export const zCreateRunRequest = z.object({
    config: zFrozenRunConfig,
    problem: zFrozenProblemInput,
    runtime: zRuntimeConfig
});

/**
 * IntakeSessionView
 */
export const zIntakeSessionView = z.object({
    convergence: z._default(z.optional(zIntakeConvergenceView), {
        audit_rejections: 0,
        finalized_by_user: false,
        rounds: 0
    }),
    conversation: z.array(zIntakeConversationEventView),
    decisions: z.array(zIntakeDecisionView),
    effort: z.string().check(z.minLength(1), z.maxLength(256)),
    frontier: z.array(zIntakeQuestionView),
    frozen_problem: z.nullish(zFrozenProblemInput),
    model: z.string().check(z.minLength(1), z.maxLength(256)),
    pending_problem_questions: z._default(z.optional(z.array(zIntakeQuestionView).check(z.maxLength(32))), []),
    pending_submission: z.nullish(zIntakePendingSubmissionView),
    problem_specifications: z.array(zIntakeProblemSpecificationView),
    revision: z.int().check(z.gte(0)),
    service_tier: z._default(z.optional(z.enum(['standard', 'fast'])), 'standard'),
    session_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    status: z.enum([
        'active',
        'convergence_required',
        'candidate_ready',
        'confirmed',
        'cancelled'
    ]),
    thread_generations: z.array(zIntakeThreadGenerationView)
});

/**
 * ProblemPresetView
 */
export const zProblemPresetView = z.object({
    description: z.string().check(z.minLength(1), z.maxLength(32768)),
    id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    problem: zFrozenProblemInput,
    title: z.string().check(z.minLength(1), z.maxLength(32768))
});

/**
 * ProblemPresetsView
 */
export const zProblemPresetsView = z.object({
    capability_profile: z._default(z.optional(z.literal('source_reading_v1')), 'source_reading_v1'),
    method_references: z.array(z.string().check(z.minLength(1), z.maxLength(32768))),
    method_source_pack: zSourcePackRef,
    presets: z.array(zProblemPresetView)
});

/**
 * StatusHistoryItem
 */
export const zStatusHistoryItem = z.object({
    seq: z.int().check(z.gte(1)),
    status: z.enum([
        'active',
        'paused',
        'parked',
        'completed',
        'killed'
    ])
});

/**
 * BranchView
 */
export const zBranchView = z.object({
    anchor_step_revision_id: z.nullable(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    branch_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    kind: z.enum([
        'root',
        'model_fork',
        'human_direction',
        'human_revision',
        'model_revision',
        'instrument_retry'
    ]),
    parent_branch_id: z.nullable(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    status: z.enum([
        'active',
        'paused',
        'parked',
        'completed',
        'killed'
    ]),
    status_history: z.array(zStatusHistoryItem),
    step_revision_ids: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)))
});

/**
 * RouteView
 */
export const zRouteView = z.object({
    branch_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    label: z.string().check(z.minLength(1), z.maxLength(256)),
    nodeIds: z.array(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    status: z.enum([
        'complete',
        'active',
        'proposed',
        'failed'
    ]),
    status_history: z.array(zStatusHistoryItem)
});

/**
 * StepChecks
 */
export const zStepChecks = z.object({
    physics: z.enum([
        'passed',
        'failed',
        'pending',
        'not_requested'
    ]),
    provenance: z.enum([
        'passed',
        'failed',
        'pending'
    ]),
    schema: z.enum([
        'passed',
        'failed',
        'pending'
    ])
});

/**
 * StepContent
 */
export const zStepContent = z.object({
    claim: z.string().check(z.minLength(1), z.maxLength(32768)),
    derivation: z.string().check(z.minLength(1), z.maxLength(32768)),
    scope: z.string().check(z.minLength(1), z.maxLength(32768)),
    source: z.string().check(z.minLength(1), z.maxLength(32768)),
    why: z.string().check(z.minLength(1), z.maxLength(32768))
});

/**
 * StepProvenance
 */
export const zStepProvenance = z.object({
    createdAt: z.string(),
    model: z.string(),
    threadId: z.string(),
    turnId: z.string()
});

/**
 * StepView
 *
 * One sealed StepRevision as the product shows it.
 *
 * ``output_sha256`` always identifies the *recorded* content, even when
 * ``typeset`` is true: the sealed text is the Record, and a typeset layer is
 * a display artifact derived from it, never a new version of it.
 */
export const zStepView = z.object({
    branch_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    checks: zStepChecks,
    content: z.nullable(zStepContent),
    id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    input: z.string(),
    order: z.int().check(z.gte(0)),
    output: z.string(),
    output_sha256: z.nullable(z.string().check(z.regex(/^[0-9a-f]{64}$/))),
    provenance: z.nullable(zStepProvenance),
    reasoningSummary: z.string(),
    revisionId: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    status: z.enum([
        'sealed',
        'running',
        'proposed',
        'failed'
    ]),
    title: z.string().check(z.minLength(1), z.maxLength(256)),
    typeset: z._default(z.optional(z.boolean()), false)
});

/**
 * SubmitIntakeRoundRequest
 */
export const zSubmitIntakeRoundRequest = z.object({
    answers: z._default(z.optional(z.record(z.string(), z.unknown())), {}),
    base_revision: z.int().check(z.gte(0)),
    user_message: z.nullish(z.string().check(z.minLength(1), z.maxLength(32768)))
});

/**
 * TypesetLayerView
 *
 * Identity of the typeset layer one route's shown text was taken from.
 *
 * ``content_sha256`` is the layer's own content hash, so a client can tell
 * two renderings of the same Record apart, and ``status`` is the layer's
 * compile verdict (a layer delivered with failures still repairs the
 * formulas it could).
 */
export const zTypesetLayerView = z.object({
    content_sha256: z.string().check(z.regex(/^[0-9a-f]{64}$/)),
    route_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    status: z.string().check(z.minLength(1), z.maxLength(256))
});

/**
 * RunView
 *
 * Canonical complete snapshot plus backend-derived control phase.
 *
 * A real service must build this from the last strict-replayable Record V1
 * head.  In-flight deltas belong only in ``RunEvent.overlay`` and must not be
 * promoted into ``steps`` before a StepRevision is sealed.
 *
 * ``typeset_layers`` is the one place where this snapshot is not the sealed
 * text: the routes listed there have their math fragments rendered from a
 * verified typeset layer, and the steps carrying that text say so with
 * ``StepView.typeset``.
 */
export const zRunView = z.object({
    branches: z.array(zBranchView),
    budget: zBudgetView,
    canonical_event_id: z.int().check(z.gte(0), z.lte(9007199254740991)),
    commands: zRunCommandCapabilities,
    config: zFrozenRunConfig,
    created_at: z.string(),
    edges: z.array(zEdgeView),
    errorMessage: z.nullish(z.string()),
    hard_interrupt_requested: z.boolean(),
    id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    pause_requested: z.boolean(),
    phase: z.enum([
        'submitted',
        'autonomous_exploration',
        'human_expansion',
        'paused',
        'recovering',
        'review_ready',
        'review_ready_due_to_cap',
        'interrupted',
        'error'
    ]),
    question: z.string().check(z.minLength(1), z.maxLength(32768)),
    read_only: z._default(z.boolean(), false),
    rootStepId: z.nullable(z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/))),
    routes: z.array(zRouteView),
    runtime: zRuntimeConfig,
    status: z.enum([
        'running',
        'review_ready',
        'paused',
        'interrupted',
        'error'
    ]),
    steps: z.array(zStepView),
    typeset_layers: z.optional(z.array(zTypesetLayerView)),
    updated_at: z.string()
});

/**
 * RunEvent
 */
export const zRunEvent = z.object({
    event_id: z.int().check(z.gte(1), z.lte(9007199254740991)),
    occurred_at: z.string(),
    overlay: z.nullable(zRuntimeOverlay),
    run: z.nullable(zRunView),
    run_id: z.string().check(z.regex(/^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$/)),
    type: z.enum([
        'run.created',
        'run.updated',
        'step.sealed',
        'branch.created',
        'run.error'
    ])
});

/**
 * Successful Response
 */
export const zAccountApiAccountGetResponse = zAccountView;

/**
 * Successful Response
 */
export const zStartDeviceLoginApiAccountDeviceLoginStartPostResponse = zDeviceLoginStartView;

/**
 * Successful Response
 */
export const zGetDeviceLoginApiAccountDeviceLoginLoginIdGetResponse = zDeviceLoginStatusView;

/**
 * Successful Response
 */
export const zCancelDeviceLoginApiAccountDeviceLoginLoginIdCancelPostResponse = zDeviceLoginCancelView;

/**
 * Command accepted.
 */
export const zImportExistingAccountApiAccountImportExistingPostResponse = zAccountView;

/**
 * Successful Response
 */
export const zAccountRateLimitsApiAccountRateLimitsGetResponse = zAccountRateLimitsView;

/**
 * Successful Response
 */
export const zBuildInfoApiBuildInfoGetResponse = zBuildInfoView;

/**
 * Successful Response
 */
export const zCapabilitiesApiCapabilitiesGetResponse = zCapabilitiesView;

/**
 * Successful Response
 */
export const zQuitReadinessApiDesktopQuitReadinessGetResponse = zQuitReadinessView;

/**
 * Response List Intake Sessions Api Intake Sessions Get
 *
 * Successful Response
 */
export const zListIntakeSessionsApiIntakeSessionsGetResponse = z.array(zIntakeSessionView);

/**
 * Command accepted.
 */
export const zCreateIntakeSessionApiIntakeSessionsPostResponse = zIntakeSessionView;

/**
 * Successful Response
 */
export const zGetIntakeSessionApiIntakeSessionsSessionIdGetResponse = zIntakeSessionView;

/**
 * Command accepted.
 */
export const zCancelIntakeSessionApiIntakeSessionsSessionIdCancelPostResponse = zIntakeSessionView;

/**
 * Command accepted.
 */
export const zConfirmIntakeSessionApiIntakeSessionsSessionIdConfirmPostResponse = zIntakeSessionView;

/**
 * Command accepted.
 */
export const zFinalizeIntakeSessionApiIntakeSessionsSessionIdFinalizePostResponse = zIntakeSessionView;

/**
 * Command accepted.
 */
export const zSubmitIntakeRoundApiIntakeSessionsSessionIdRoundsPostResponse = zIntakeSessionView;

/**
 * Successful Response
 */
export const zProblemPresetsApiProblemPresetsGetResponse = zProblemPresetsView;

/**
 * Response List Runs Api Runs Get
 *
 * Successful Response
 */
export const zListRunsApiRunsGetResponse = z.array(zRunSummary);

/**
 * Command accepted.
 */
export const zCreateRunApiRunsPostResponse = zRunView;

/**
 * Successful Response
 */
export const zGetRunApiRunsRunIdGetResponse = zRunView;

/**
 * Command accepted.
 */
export const zCreateBranchApiRunsRunIdBranchesPostResponse = zRunView;

/**
 * Replayable Server-Sent Events stream.
 */
export const zEventsApiRunsRunIdEventsGetResponse = z.string();

/**
 * Command accepted.
 */
export const zInterruptRunApiRunsRunIdInterruptPostResponse = zRunView;

/**
 * Command accepted.
 */
export const zPauseRunApiRunsRunIdPausePostResponse = zRunView;

/**
 * Successful Response
 */
export const zExportReportApiRunsRunIdReportsPostResponse = zReportBundleView;

/**
 * Command accepted.
 */
export const zResumeRunApiRunsRunIdResumePostResponse = zRunView;

/**
 * Response List Site Accounts Api Site Admin Accounts Get
 *
 * Successful Response
 */
export const zListSiteAccountsApiSiteAdminAccountsGetResponse = z.array(zSiteAccountView);

/**
 * Successful Response
 */
export const zCreateSiteAccountApiSiteAdminAccountsPostResponse = zSiteAccountView;

/**
 * Response List Admin Site Account Intakes Api Site Admin Accounts  User Id  Intake Sessions Get
 *
 * Successful Response
 */
export const zListAdminSiteAccountIntakesApiSiteAdminAccountsUserIdIntakeSessionsGetResponse = z.array(zIntakeSessionView);

/**
 * Successful Response
 */
export const zGetAdminSiteAccountIntakeApiSiteAdminAccountsUserIdIntakeSessionsSessionIdGetResponse = zIntakeSessionView;

/**
 * Successful Response
 */
export const zResetSiteAccountPasswordApiSiteAdminAccountsUserIdPasswordResetPostResponse = z.void();

/**
 * Response List Admin Site Account Runs Api Site Admin Accounts  User Id  Runs Get
 *
 * Successful Response
 */
export const zListAdminSiteAccountRunsApiSiteAdminAccountsUserIdRunsGetResponse = z.array(zRunSummary);

/**
 * Successful Response
 */
export const zGetAdminSiteAccountRunApiSiteAdminAccountsUserIdRunsRunIdGetResponse = zRunView;

/**
 * Successful Response
 */
export const zSetSiteAccountStatusApiSiteAdminAccountsUserIdStatusPostResponse = zSiteAccountView;

/**
 * Successful Response
 */
export const zSiteModeApiSiteModeGetResponse = zSiteModeView;

/**
 * Successful Response
 */
export const zChangeSitePasswordApiSitePasswordPostResponse = z.void();

/**
 * Successful Response
 */
export const zDeleteSiteSessionApiSiteSessionDeleteResponse = z.void();

/**
 * Successful Response
 */
export const zGetSiteSessionApiSiteSessionGetResponse = zSiteSessionView;

/**
 * Successful Response
 */
export const zCreateSiteSessionApiSiteSessionPostResponse = zSiteSessionView;

/**
 * Successful Response
 */
export const zHealthHealthzGetResponse = zHealthView;
